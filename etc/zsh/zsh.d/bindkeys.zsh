bindkey '^[[A' up-line-or-search
bindkey '^[[B' down-line-or-search

bindkey "[D" backward-word
bindkey "[C" forward-word

bindkey "^[a" beginning-of-line # Iterm2 preferences: Command + Left arrow => Send ^[a
bindkey "^[e" end-of-line       # Iterm2 preferences: Command + Right arrow => Send ^[e
